# Server
PORT=9999
HOST=0.0.0.0
NODE_ENV=production

# CORS: comma-separated allowed origins (only in production). Leave unset in dev for permissive CORS.
# CORS_ORIGIN=https://your-frontend.vercel.app,https://your-domain.com
